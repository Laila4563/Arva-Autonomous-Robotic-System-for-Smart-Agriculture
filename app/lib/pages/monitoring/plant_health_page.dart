import 'package:flutter/material.dart';

class PlantHealthPage extends StatelessWidget {
  const PlantHealthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Plant Health Page")),
      body: const Center(child: Text("Plant Health Data Coming Soon")), // SR05 logic goes here
    );
  }
}