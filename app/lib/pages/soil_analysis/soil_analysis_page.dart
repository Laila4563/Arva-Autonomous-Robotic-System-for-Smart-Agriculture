import 'package:flutter/material.dart';

class SoilAnalysisPage extends StatelessWidget {
  const SoilAnalysisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Soil Analysis Page")),
      body: const Center(child: Text("NPK Sensor Data Coming Soon")), // SR05 logic goes here
    );
  }
}